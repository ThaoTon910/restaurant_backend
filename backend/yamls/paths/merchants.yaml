# paths/merchants.yaml
post:
  summary: "create a new merchant"
  description: ""
  operationId: app.create_merchant
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../schemas/merchant.yaml"

  responses:
    $ref: "../responses/merchants.yaml#/post"
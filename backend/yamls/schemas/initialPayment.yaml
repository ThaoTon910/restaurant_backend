# schemas/initialPayment.yaml

initialPayment:
  type: object
  required:
    - amount

  properties:
    amount:
      type: number
      description: amount wish to make in this payment

clientSecret:
  type: object
  required:
    - clientSecret

  properties:
    clientSecret:
      type: string
      description: client secret to process the other